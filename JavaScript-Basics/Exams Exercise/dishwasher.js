function dishwasher(input) {
    let countBottles = Number(input[0]);
    let totalDetergent = countBottles * 750;

    let command = input[index];
    index++;

    while(command !== "End") {
        
        
    }
}
dishwasher(["2", "53", "65", "55", "End"])